# Album Tracker - Environment Configuration
# Copy this file to .env and update the values for your deployment

# Music Library Path
# Path to your music library on the host machine
# This will be mounted as read-only into the backend container
MUSIC_LIBRARY_PATH=/path/to/your/music/library

# Backend Configuration (optional overrides)
# PORT=3035
# NODE_ENV=production
# DATABASE_PATH=/app/data/album-tracker.db

# Frontend Configuration (optional overrides)
# VITE_API_URL=http://localhost:3035
