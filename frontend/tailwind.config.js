/** @type {import('tailwindcss').Config} */
export default {
  // Tailwind CSS v4 uses CSS-first configuration
  // This file is optional but kept for potential theme extensions
  theme: {
    extend: {},
  },
  plugins: [],
}
